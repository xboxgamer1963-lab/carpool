---
import Layout from "../layouts/Layout.astro";
import DriverCard from "../components/DriverCard.astro";
import { listDrivers, type Driver } from "../lib/drivers";

// Live data powers the stats, popular routes and recent rides. Fails soft so the
// page always renders even if Firestore is unreachable.
let drivers: Driver[] = [];
try {
  drivers = await listDrivers();
} catch {
  drivers = [];
}

const rideCount = drivers.length;
const seatCount = drivers.reduce((n, d) => n + (d.seats || 0), 0);
const areaCount = new Set(
  drivers.flatMap((d) =>
    [d.fromLocation, d.toLocation, ...d.stops]
      .map((s) => s.trim().toLowerCase())
      .filter(Boolean)
  )
).size;

// Most-offered routes, for quick-search chips.
const routeCounts = new Map<string, { from: string; to: string; n: number }>();
for (const d of drivers) {
  if (!d.fromLocation || !d.toLocation) continue;
  const key = `${d.fromLocation.toLowerCase()}→${d.toLocation.toLowerCase()}`;
  const cur = routeCounts.get(key);
  if (cur) cur.n++;
  else routeCounts.set(key, { from: d.fromLocation, to: d.toLocation, n: 1 });
}
const popularRoutes = [...routeCounts.values()]
  .sort((a, b) => b.n - a.n)
  .slice(0, 6);

const recentRides = drivers.slice(0, 3);

const stats = [
  { value: rideCount, label: "Rides on offer", suffix: "" },
  { value: seatCount, label: "Seats available", suffix: "" },
  { value: areaCount, label: "Areas covered", suffix: "" },
  { value: 0, label: "Commission charged", prefix: "", suffix: "%" },
];

const faqs = [
  {
    q: "Is Carpool free to use?",
    a: "Yes. Listing a ride and finding one are both completely free. Any fare or fuel contribution is agreed directly between the driver and rider — we never take a cut.",
  },
  {
    q: "How do I contact a driver?",
    a: "Every ride card has a one-tap WhatsApp button (with a pre-filled message) and a call button. You arrange pickup points, timing and cost between yourselves.",
  },
  {
    q: "Do I need an account to search?",
    a: "No — browsing and searching rides is open to everyone. You only sign in when you want to publish a ride as a driver.",
  },
  {
    q: "How accurate are the pickup locations?",
    a: "Drivers pin their exact pickup and drop-off on a map when they register, so what you see on the map is where they actually start and end.",
  },
];
---

<Layout title="Carpool — share your daily commute">
  <!-- ================= HERO ================= -->
  <section class="hero">
    <div class="hero-copy" data-reveal>
      <span class="eyebrow">Daily commute, shared</span>
      <h1>
        Going the same way?<br />
        <span class="hl">Share the ride.</span>
      </h1>
      <p class="sub">
        Drivers post their route and timings. Riders search by where they live
        and where they’re headed — then connect directly to split the cost.
      </p>

      <form class="search" method="GET" action="/drivers">
        <div class="search-fields">
          <div class="field">
            <label for="from">From</label>
            <div class="input-icon">
              <span class="pin pin-start" aria-hidden="true"></span>
              <input id="from" name="from" placeholder="Your home area" />
            </div>
          </div>
          <span class="search-arrow" aria-hidden="true">→</span>
          <div class="field">
            <label for="to">To</label>
            <div class="input-icon">
              <span class="pin pin-end" aria-hidden="true"></span>
              <input id="to" name="to" placeholder="Drop-off area" />
            </div>
          </div>
        </div>
        <button type="submit" class="btn btn-primary search-btn">
          Find rides
        </button>
      </form>

      {
        popularRoutes.length > 0 && (
          <div class="quick-routes">
            <span class="muted">Popular:</span>
            {popularRoutes.slice(0, 4).map((r) => (
              <a
                class="route-chip"
                href={`/drivers?from=${encodeURIComponent(r.from)}&to=${encodeURIComponent(r.to)}`}
              >
                {r.from} → {r.to}
              </a>
            ))}
          </div>
        )
      }

      <div class="under-search">
        <button type="button" id="near-me" class="near-me-btn">
          <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
            <path d="M12 21s-7-5.2-7-11a7 7 0 1 1 14 0c0 5.8-7 11-7 11Z" stroke-linejoin="round"></path>
            <circle cx="12" cy="10" r="2.4"></circle>
          </svg>
          <span id="near-me-label">Find rides near me</span>
        </button>
        <span class="muted dot-sep">
          {rideCount > 0 && `${rideCount} ${rideCount === 1 ? "ride" : "rides"} available · `}
          <a href="/drivers">Browse all →</a>
        </span>
      </div>
    </div>

    <div class="hero-card" aria-hidden="true" data-reveal>
      <div class="map-card card">
        <div class="route-line">
          <span class="node start"></span>
          <span class="thread"></span>
          <span class="node mid"></span>
          <span class="thread"></span>
          <span class="node end"></span>
          <span class="drive-car">
            <svg viewBox="0 0 40 24" width="36" height="22">
              <path d="M3 16c-1.1 0-2-.9-2-2v-1.5c0-.8.4-1.5 1.1-1.9l3-1.7 3.2-3.1C9 4.3 9.9 4 10.8 4h13.4c.9 0 1.8.4 2.4 1.1l3.4 4 5.1 1.2c1.1.3 1.9 1.3 1.9 2.4V14c0 1.1-.9 2-2 2H3Z" fill="#16704a"></path>
              <path d="M11 5.5h12.6c.6 0 1.1.2 1.5.7l2.7 3.1H8.2l2-2.7c.2-.7.4-1.1.8-1.1Z" fill="#cfe7d6"></path>
              <circle cx="11" cy="16.5" r="3.2" fill="#1d1b14"></circle>
              <circle cx="11" cy="16.5" r="1.3" fill="#fff"></circle>
              <circle cx="29" cy="16.5" r="3.2" fill="#1d1b14"></circle>
              <circle cx="29" cy="16.5" r="1.3" fill="#fff"></circle>
            </svg>
          </span>
        </div>
        <div class="route-labels">
          <div><strong>Gulshan</strong><span class="muted">08:00 pickup</span></div>
          <div class="ml-auto right">
            <strong>Downtown</strong><span class="muted">08:35 arrival</span>
          </div>
        </div>
        <div class="map-driver">
          <div class="dp">A</div>
          <div>
            <strong>Ayesha · Corolla</strong>
            <span class="muted">3 seats · Mon–Fri</span>
          </div>
          <span class="tag">Rs 5k/mo</span>
        </div>
      </div>
    </div>
  </section>

  <!-- ================= STATS ================= -->
  <section class="stats" data-reveal>
    {
      stats.map((s) => (
        <div class="stat">
          <span class="stat-num" data-count={s.value} data-suffix={s.suffix ?? ""}>
            {s.prefix ?? ""}0{s.suffix ?? ""}
          </span>
          <span class="stat-label muted">{s.label}</span>
        </div>
      ))
    }
  </section>

  <!-- ================= HOW IT WORKS ================= -->
  <section class="band">
    <header class="section-head" data-reveal>
      <span class="kicker">Simple by design</span>
      <h2>How it works</h2>
      <p class="muted">Three steps from stranger to carpool.</p>
    </header>
    <div class="steps">
      {
        [
          {
            n: "01",
            t: "Drivers register",
            d: "Car owners sign in and pin their car, route, timings and a contact number in under a minute.",
          },
          {
            n: "02",
            t: "Riders search",
            d: "Search by home and drop-off to instantly see drivers heading your way on your schedule.",
          },
          {
            n: "03",
            t: "Connect directly",
            d: "Call or WhatsApp the driver and finalise the arrangement between yourselves. No middleman.",
          },
        ].map((s, i) => (
          <article class="card step" data-reveal style={`--d:${i * 80}ms`}>
            <span class="num">{s.n}</span>
            <h3>{s.t}</h3>
            <p class="muted">{s.d}</p>
          </article>
        ))
      }
    </div>
  </section>

  <!-- ================= AUDIENCE SPLIT ================= -->
  <section class="split">
    <article class="split-card drivers card" data-reveal>
      <span class="split-tag">For drivers</span>
      <h2>Empty seats?<br />Fill them.</h2>
      <p class="muted">
        You’re already making the drive. Offer your spare seats, cover your fuel,
        and share the road with people going your way.
      </p>
      <ul class="ticks">
        <li>Pin your exact pickup &amp; drop-off on the map</li>
        <li>Set your days, times and fare</li>
        <li>Riders reach you on WhatsApp — you stay in control</li>
      </ul>
      <a href="/drivers/register" class="btn btn-primary">Offer a ride</a>
    </article>

    <article class="split-card riders card" data-reveal style="--d:80ms">
      <span class="split-tag amber">For riders</span>
      <h2>Skip the wait.<br />Ride together.</h2>
      <p class="muted">
        Find someone already driving your route. Cheaper than a daily cab, kinder
        to the planet, and a familiar face for the commute.
      </p>
      <ul class="ticks">
        <li>Search by your home and destination</li>
        <li>See drivers, seats, timings and fare upfront</li>
        <li>Watch the route on an interactive map before you commit</li>
      </ul>
      <a href="/drivers" class="btn btn-ghost">Find a ride</a>
    </article>
  </section>

  <!-- ================= POPULAR ROUTES ================= -->
  {
    popularRoutes.length > 0 && (
      <section class="band">
        <header class="section-head" data-reveal>
          <span class="kicker">Trending</span>
          <h2>Popular routes</h2>
          <p class="muted">Where the carpools are heading right now.</p>
        </header>
        <div class="route-grid" data-reveal>
          {popularRoutes.map((r) => (
            <a
              class="route-tile"
              href={`/drivers?from=${encodeURIComponent(r.from)}&to=${encodeURIComponent(r.to)}`}
            >
              <div class="rt-line">
                <span class="rt-dot start" />
                <span class="rt-track" />
                <span class="rt-dot end" />
              </div>
              <div class="rt-names">
                <strong>{r.from}</strong>
                <span class="muted">to {r.to}</span>
              </div>
              <span class="rt-count">{r.n}</span>
            </a>
          ))}
        </div>
      </section>
    )
  }

  <!-- ================= RECENT RIDES ================= -->
  {
    recentRides.length > 0 && (
      <section class="band">
        <header class="section-head row-head" data-reveal>
          <div>
            <span class="kicker">Fresh</span>
            <h2>Recently added rides</h2>
          </div>
          <a href="/drivers" class="btn btn-ghost btn-sm">View all</a>
        </header>
        <div class="recent-grid">
          {recentRides.map((d, i) => (
            <div data-reveal style={`--d:${i * 80}ms`}>
              <DriverCard driver={d} />
            </div>
          ))}
        </div>
      </section>
    )
  }

  <!-- ================= BENEFITS ================= -->
  <section class="band">
    <header class="section-head" data-reveal>
      <span class="kicker">Why carpool</span>
      <h2>Better for your wallet, your city, the planet</h2>
    </header>
    <div class="benefits">
      {
        [
          { i: "💸", t: "Spend less", d: "Split fuel and parking instead of paying for a daily cab or running a second car." },
          { i: "🌍", t: "Cut emissions", d: "One shared car means fewer on the road. Every full seat is a car someone didn’t drive." },
          { i: "🚦", t: "Beat the traffic", d: "Fewer cars, smoother roads, and the carpool lane when your city has one." },
          { i: "🤝", t: "Know your ride", d: "Recurring routes mean familiar faces — not a different stranger every day." },
        ].map((b, i) => (
          <article class="benefit" data-reveal style={`--d:${i * 70}ms`}>
            <span class="b-icon">{b.i}</span>
            <h3>{b.t}</h3>
            <p class="muted">{b.d}</p>
          </article>
        ))
      }
    </div>
  </section>

  <!-- ================= TESTIMONIALS ================= -->
  <section class="band">
    <header class="section-head" data-reveal>
      <span class="kicker">From the road</span>
      <h2>Commuters love it</h2>
    </header>
    <div class="quotes">
      {
        [
          { q: "I cut my monthly commute cost by more than half and stopped fighting for parking. Wish I’d done it sooner.", n: "Bilal", r: "Driver · Clifton → I.I. Chundrigar" },
          { q: "Found a ride from my exact neighbourhood in two minutes. The map made it obvious who was actually nearby.", n: "Sana", r: "Rider · Gulshan → Saddar" },
          { q: "Same three people every morning now. It’s gone from a chore to the best part of my day.", n: "Hamza", r: "Driver · DHA → Korangi" },
        ].map((t, i) => (
          <figure class="quote card" data-reveal style={`--d:${i * 80}ms`}>
            <span class="mark" aria-hidden="true">“</span>
            <blockquote>{t.q}</blockquote>
            <figcaption>
              <span class="qa">{t.n.charAt(0)}</span>
              <span>
                <strong>{t.n}</strong>
                <span class="muted">{t.r}</span>
              </span>
            </figcaption>
          </figure>
        ))
      }
    </div>
  </section>

  <!-- ================= FAQ ================= -->
  <section class="band faq-band">
    <header class="section-head" data-reveal>
      <span class="kicker">Good to know</span>
      <h2>Questions, answered</h2>
    </header>
    <div class="faqs" data-reveal>
      {
        faqs.map((f) => (
          <details class="faq">
            <summary>
              {f.q}
              <span class="chev" aria-hidden="true">+</span>
            </summary>
            <p class="muted">{f.a}</p>
          </details>
        ))
      }
    </div>
  </section>

  <!-- ================= CTA ================= -->
  <section class="cta" data-reveal>
    <div class="cta-inner">
      <h2>Your next commute could be shared.</h2>
      <p>Offer your empty seats or find a ride going your way — it’s free to start.</p>
      <div class="cta-actions">
        <a href="/drivers/register" class="btn btn-light">Offer a ride</a>
        <a href="/drivers" class="btn btn-outline">Find a ride</a>
      </div>
    </div>
  </section>
</Layout>

<script>
  // Scroll reveal
  const revealEls = document.querySelectorAll<HTMLElement>("[data-reveal]");
  const reduce = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
  if (reduce) {
    revealEls.forEach((el) => el.classList.add("in"));
  } else {
    const io = new IntersectionObserver(
      (entries) => {
        entries.forEach((e) => {
          if (e.isIntersecting) {
            e.target.classList.add("in");
            io.unobserve(e.target);
          }
        });
      },
      { threshold: 0.12, rootMargin: "0px 0px -40px 0px" }
    );
    revealEls.forEach((el) => io.observe(el));
  }

  // Animated stat counters
  const counters = document.querySelectorAll<HTMLElement>(".stat-num[data-count]");
  const animate = (el: HTMLElement) => {
    const target = Number(el.dataset.count || "0");
    const suffix = el.dataset.suffix || "";
    if (reduce || target === 0) {
      el.textContent = `${target}${suffix}`;
      return;
    }
    const dur = 1200;
    const start = performance.now();
    const tick = (now: number) => {
      const t = Math.min(1, (now - start) / dur);
      const eased = 1 - Math.pow(1 - t, 3);
      el.textContent = `${Math.round(target * eased)}${suffix}`;
      if (t < 1) requestAnimationFrame(tick);
    };
    requestAnimationFrame(tick);
  };
  const cio = new IntersectionObserver(
    (entries) => {
      entries.forEach((e) => {
        if (e.isIntersecting) {
          animate(e.target as HTMLElement);
          cio.unobserve(e.target);
        }
      });
    },
    { threshold: 0.5 }
  );
  counters.forEach((c) => cio.observe(c));

  // "Find rides near me" — capture location, then hand coords to the browse page.
  const nearBtn = document.getElementById("near-me");
  const nearLabel = document.getElementById("near-me-label");
  nearBtn?.addEventListener("click", () => {
    if (!navigator.geolocation) {
      window.location.href = "/drivers";
      return;
    }
    if (nearLabel) nearLabel.textContent = "Locating…";
    (nearBtn as HTMLButtonElement).disabled = true;
    navigator.geolocation.getCurrentPosition(
      (pos) => {
        const { latitude, longitude } = pos.coords;
        window.location.href = `/drivers?lat=${latitude}&lng=${longitude}`;
      },
      () => {
        // Denied or failed — just take them to all rides.
        window.location.href = "/drivers";
      },
      { enableHighAccuracy: true, timeout: 8000 }
    );
  });
</script>

<style>
  /* ---------- Reveal ---------- */
  [data-reveal] {
    opacity: 0;
    transform: translateY(18px);
    transition:
      opacity 0.6s ease,
      transform 0.6s cubic-bezier(0.2, 0.8, 0.2, 1);
    transition-delay: var(--d, 0ms);
  }
  [data-reveal].in {
    opacity: 1;
    transform: none;
  }
  @media (prefers-reduced-motion: reduce) {
    [data-reveal] {
      opacity: 1;
      transform: none;
    }
  }

  /* ---------- Hero ---------- */
  .hero {
    display: grid;
    grid-template-columns: 1.15fr 0.85fr;
    gap: 2.5rem;
    align-items: center;
    padding: 1.5rem 0 3rem;
  }
  .eyebrow {
    display: inline-block;
    font-weight: 600;
    font-size: 0.8rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--green);
    background: var(--green-soft);
    padding: 0.35rem 0.7rem;
    border-radius: var(--radius-pill);
    margin-bottom: 1.1rem;
  }
  h1 {
    font-size: clamp(2.4rem, 5.5vw, 3.9rem);
    font-weight: 800;
  }
  .hl {
    color: var(--green);
    position: relative;
  }
  .hl::after {
    content: "";
    position: absolute;
    left: 0;
    right: 0;
    bottom: 0.08em;
    height: 0.32em;
    background: var(--amber-soft);
    z-index: -1;
    border-radius: 2px;
  }
  .sub {
    max-width: 34rem;
    margin: 1.1rem 0 1.8rem;
    color: var(--ink-2);
    font-size: 1.1rem;
  }
  .search {
    display: flex;
    gap: 0.6rem;
    background: var(--surface);
    border: 1px solid var(--line-2);
    border-radius: var(--radius);
    box-shadow: var(--shadow);
    padding: 0.6rem;
  }
  .search-fields {
    flex: 1;
    display: flex;
    align-items: flex-end;
    gap: 0.4rem;
  }
  .search .field {
    flex: 1;
  }
  .search label {
    margin: 0 0 0.2rem 0.2rem;
    font-size: 0.72rem;
    text-transform: uppercase;
    letter-spacing: 0.06em;
  }
  .input-icon {
    position: relative;
  }
  .input-icon input {
    border: none;
    background: transparent;
    padding-left: 1.5rem;
    box-shadow: none;
  }
  .input-icon input:focus {
    box-shadow: none;
    background: transparent;
  }
  .pin {
    position: absolute;
    left: 0.35rem;
    top: 50%;
    transform: translateY(-50%);
    width: 9px;
    height: 9px;
    border-radius: 50%;
  }
  .pin-start {
    background: var(--green);
  }
  .pin-end {
    background: var(--amber);
  }
  .search-arrow {
    color: var(--ink-3);
    font-size: 1.1rem;
    padding-bottom: 0.7rem;
  }
  .search-btn {
    flex-shrink: 0;
  }
  .quick-routes {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.45rem;
    margin-top: 1.1rem;
    font-size: 0.9rem;
  }
  .route-chip {
    background: var(--surface);
    border: 1px solid var(--line-2);
    color: var(--ink-2);
    padding: 0.3rem 0.7rem;
    border-radius: var(--radius-pill);
    font-weight: 600;
    font-size: 0.82rem;
    transition:
      border-color 0.15s ease,
      color 0.15s ease;
  }
  .route-chip:hover {
    border-color: var(--green);
    color: var(--green);
    text-decoration: none;
  }
  .under-search {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.6rem 0.9rem;
    margin-top: 1.1rem;
    font-size: 0.95rem;
  }
  .near-me-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    font: inherit;
    font-weight: 600;
    font-size: 0.9rem;
    color: var(--green);
    background: var(--surface);
    border: 1px solid var(--line-2);
    padding: 0.45rem 0.85rem;
    border-radius: var(--radius-pill);
    cursor: pointer;
    box-shadow: var(--shadow-sm);
    transition:
      border-color 0.15s ease,
      transform 0.15s ease;
  }
  .near-me-btn:hover {
    border-color: var(--green);
    transform: translateY(-1px);
  }
  .near-me-btn:disabled {
    opacity: 0.7;
    cursor: default;
  }
  .dot-sep {
    font-size: 0.95rem;
  }

  .hero-card {
    display: flex;
    justify-content: center;
  }
  .map-card {
    width: 100%;
    max-width: 360px;
    box-shadow: var(--shadow-lg);
    transform: rotate(1.4deg);
    background:
      radial-gradient(20rem 12rem at 120% -10%, var(--green-soft), transparent 70%),
      var(--surface);
  }
  .route-line {
    position: relative;
    display: flex;
    align-items: center;
    margin-bottom: 0.6rem;
  }
  .drive-car {
    position: absolute;
    top: 50%;
    left: 0;
    transform: translate(0, -60%);
    filter: drop-shadow(0 3px 3px rgba(20, 30, 20, 0.3));
    animation: drive 4.5s cubic-bezier(0.45, 0, 0.55, 1) infinite;
  }
  @keyframes drive {
    0% { left: 2%; opacity: 0; }
    12% { opacity: 1; }
    85% { opacity: 1; }
    100% { left: 88%; opacity: 0; }
  }
  @media (prefers-reduced-motion: reduce) {
    .drive-car { animation: none; left: 44%; }
  }
  .node {
    width: 13px;
    height: 13px;
    border-radius: 50%;
    border: 3px solid var(--surface);
    box-shadow: 0 0 0 1.5px currentColor;
    flex-shrink: 0;
  }
  .node.start { color: var(--green); background: var(--green); }
  .node.mid { color: var(--line-2); background: var(--line-2); width: 9px; height: 9px; }
  .node.end { color: var(--amber); background: var(--amber); }
  .thread {
    flex: 1;
    height: 2px;
    background: repeating-linear-gradient(to right, var(--line-2) 0 6px, transparent 6px 11px);
  }
  .route-labels { display: flex; font-size: 0.85rem; }
  .route-labels strong { display: block; }
  .route-labels .muted { font-size: 0.78rem; }
  .route-labels .right { text-align: right; }
  .ml-auto { margin-left: auto; }
  .map-driver {
    display: flex;
    align-items: center;
    gap: 0.7rem;
    margin-top: 1.1rem;
    padding-top: 1.1rem;
    border-top: 1px solid var(--line);
    font-size: 0.85rem;
  }
  .map-driver strong { display: block; }
  .map-driver .muted { font-size: 0.78rem; }
  .dp {
    display: grid;
    place-items: center;
    width: 38px;
    height: 38px;
    border-radius: 50%;
    background: linear-gradient(150deg, var(--amber), #b9661a);
    color: #fff;
    font-family: var(--font-display);
    font-weight: 700;
    flex-shrink: 0;
  }
  .tag {
    margin-left: auto;
    background: var(--green-soft);
    color: var(--green-ink);
    font-weight: 700;
    font-size: 0.75rem;
    padding: 0.25rem 0.55rem;
    border-radius: var(--radius-pill);
    white-space: nowrap;
  }

  /* ---------- Section scaffolding ---------- */
  .band {
    padding: 3rem 0;
    border-top: 1px solid var(--line);
  }
  .section-head {
    text-align: center;
    max-width: 40rem;
    margin: 0 auto 2rem;
  }
  .section-head.row-head {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    text-align: left;
    max-width: none;
    gap: 1rem;
  }
  .kicker {
    display: inline-block;
    font-weight: 700;
    font-size: 0.74rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--amber);
    margin-bottom: 0.55rem;
  }
  .section-head h2 {
    font-size: clamp(1.7rem, 3.6vw, 2.4rem);
  }
  .section-head .muted {
    margin-top: 0.4rem;
  }

  /* ---------- Stats ---------- */
  .stats {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1rem;
    background: linear-gradient(160deg, var(--green) 0%, var(--green-dk) 100%);
    color: #fff;
    border-radius: var(--radius);
    padding: 1.75rem 1.5rem;
    box-shadow: var(--shadow-lg);
  }
  .stat {
    text-align: center;
    padding: 0.25rem;
  }
  .stat-num {
    display: block;
    font-family: var(--font-display);
    font-weight: 800;
    font-size: clamp(1.8rem, 4vw, 2.7rem);
    line-height: 1;
    letter-spacing: -0.02em;
  }
  .stat-label {
    color: rgba(255, 255, 255, 0.78) !important;
    font-size: 0.85rem;
    font-weight: 600;
    margin-top: 0.4rem;
    display: block;
  }

  /* ---------- Steps ---------- */
  .steps {
    display: grid;
    gap: 1.1rem;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
  }
  .step h3 { font-size: 1.2rem; margin: 0.7rem 0 0.45rem; }
  .step p { margin: 0; font-size: 0.96rem; }
  .num {
    font-family: var(--font-display);
    font-weight: 800;
    font-size: 1.05rem;
    color: var(--green);
    background: var(--green-soft);
    padding: 0.3rem 0.6rem;
    border-radius: 9px;
  }

  /* ---------- Split ---------- */
  .split {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.2rem;
    padding: 3rem 0;
    border-top: 1px solid var(--line);
  }
  .split-card {
    padding: 2rem;
    position: relative;
    overflow: hidden;
  }
  .split-card.drivers {
    background:
      radial-gradient(28rem 16rem at 110% -20%, var(--green-soft), transparent 70%),
      var(--surface);
  }
  .split-card.riders {
    background:
      radial-gradient(28rem 16rem at 110% -20%, var(--amber-soft), transparent 70%),
      var(--surface);
  }
  .split-tag {
    display: inline-block;
    font-weight: 700;
    font-size: 0.72rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--green);
    background: var(--green-soft);
    padding: 0.3rem 0.65rem;
    border-radius: var(--radius-pill);
    margin-bottom: 1rem;
  }
  .split-tag.amber {
    color: #9a5a14;
    background: var(--amber-soft);
  }
  .split-card h2 {
    font-size: clamp(1.6rem, 3.4vw, 2.1rem);
    margin-bottom: 0.7rem;
  }
  .ticks {
    list-style: none;
    padding: 0;
    margin: 1.2rem 0 1.6rem;
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
  }
  .ticks li {
    position: relative;
    padding-left: 1.7rem;
    font-size: 0.96rem;
    color: var(--ink-2);
  }
  .ticks li::before {
    content: "✓";
    position: absolute;
    left: 0;
    top: -1px;
    display: grid;
    place-items: center;
    width: 1.2rem;
    height: 1.2rem;
    font-size: 0.7rem;
    font-weight: 700;
    color: #fff;
    background: var(--green);
    border-radius: 50%;
  }
  .riders .ticks li::before {
    background: var(--amber);
  }

  /* ---------- Popular routes ---------- */
  .route-grid {
    display: grid;
    gap: 0.8rem;
    grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
  }
  .route-tile {
    display: flex;
    align-items: center;
    gap: 0.85rem;
    background: var(--surface);
    border: 1px solid var(--line);
    border-radius: var(--radius-sm);
    padding: 0.9rem 1rem;
    box-shadow: var(--shadow-sm);
    transition:
      transform 0.16s ease,
      border-color 0.16s ease,
      box-shadow 0.16s ease;
  }
  .route-tile:hover {
    transform: translateY(-2px);
    border-color: var(--line-2);
    box-shadow: var(--shadow);
    text-decoration: none;
  }
  .rt-line {
    display: flex;
    flex-direction: column;
    align-items: center;
    flex-shrink: 0;
  }
  .rt-dot { width: 9px; height: 9px; border-radius: 50%; }
  .rt-dot.start { background: var(--green); }
  .rt-dot.end { background: var(--amber); }
  .rt-track {
    width: 2px;
    height: 16px;
    margin: 1px 0;
    background: repeating-linear-gradient(var(--line-2) 0 3px, transparent 3px 6px);
  }
  .rt-names {
    display: flex;
    flex-direction: column;
    min-width: 0;
    flex: 1;
  }
  .rt-names strong { color: var(--ink); }
  .rt-names .muted { font-size: 0.82rem; }
  .rt-count {
    margin-left: auto;
    background: var(--green-soft);
    color: var(--green-ink);
    font-weight: 700;
    font-size: 0.8rem;
    min-width: 1.7rem;
    height: 1.7rem;
    display: grid;
    place-items: center;
    border-radius: 50%;
  }

  /* ---------- Recent ---------- */
  .recent-grid {
    display: grid;
    gap: 1.1rem;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
  }

  /* ---------- Benefits ---------- */
  .benefits {
    display: grid;
    gap: 1.1rem;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  }
  .benefit {
    background: var(--surface);
    border: 1px solid var(--line);
    border-radius: var(--radius);
    padding: 1.5rem;
    box-shadow: var(--shadow-sm);
  }
  .b-icon {
    display: grid;
    place-items: center;
    width: 46px;
    height: 46px;
    font-size: 1.4rem;
    background: var(--paper);
    border: 1px solid var(--line);
    border-radius: 12px;
    margin-bottom: 0.8rem;
  }
  .benefit h3 { font-size: 1.1rem; margin-bottom: 0.35rem; }
  .benefit p { margin: 0; font-size: 0.92rem; }

  /* ---------- Testimonials ---------- */
  .quotes {
    display: grid;
    gap: 1.1rem;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
  }
  .quote {
    display: flex;
    flex-direction: column;
    gap: 0.9rem;
    position: relative;
    padding-top: 1.6rem;
  }
  .quote .mark {
    position: absolute;
    top: 0.4rem;
    left: 1.2rem;
    font-family: var(--font-display);
    font-size: 3rem;
    line-height: 1;
    color: var(--green-soft);
  }
  .quote blockquote {
    margin: 0;
    font-size: 1rem;
    line-height: 1.5;
    color: var(--ink);
  }
  .quote figcaption {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    margin-top: auto;
  }
  .quote figcaption strong { display: block; font-size: 0.92rem; }
  .quote figcaption .muted { font-size: 0.8rem; }
  .qa {
    display: grid;
    place-items: center;
    width: 36px;
    height: 36px;
    border-radius: 50%;
    background: linear-gradient(150deg, var(--green), var(--green-dk));
    color: #fff;
    font-family: var(--font-display);
    font-weight: 700;
    flex-shrink: 0;
  }

  /* ---------- FAQ ---------- */
  .faqs {
    max-width: 44rem;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: 0.7rem;
  }
  .faq {
    background: var(--surface);
    border: 1px solid var(--line);
    border-radius: var(--radius-sm);
    padding: 0 1.2rem;
    box-shadow: var(--shadow-sm);
  }
  .faq summary {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    cursor: pointer;
    list-style: none;
    padding: 1.05rem 0;
    font-weight: 600;
    font-size: 1rem;
  }
  .faq summary::-webkit-details-marker { display: none; }
  .faq .chev {
    font-family: var(--font-display);
    font-size: 1.4rem;
    color: var(--green);
    transition: transform 0.2s ease;
    flex-shrink: 0;
  }
  .faq[open] .chev { transform: rotate(45deg); }
  .faq p {
    margin: 0;
    padding: 0 0 1.1rem;
    font-size: 0.95rem;
    line-height: 1.55;
  }

  /* ---------- CTA ---------- */
  .cta {
    margin: 3rem 0 0;
  }
  .cta-inner {
    text-align: center;
    color: #fff;
    border-radius: var(--radius);
    padding: 3rem 1.5rem;
    background:
      radial-gradient(30rem 18rem at 15% 0%, rgba(216, 132, 42, 0.35), transparent 60%),
      linear-gradient(160deg, var(--green) 10%, var(--green-dk) 90%);
    box-shadow: var(--shadow-lg);
  }
  .cta-inner h2 {
    font-size: clamp(1.7rem, 4vw, 2.5rem);
  }
  .cta-inner p {
    margin: 0.6rem auto 1.5rem;
    max-width: 32rem;
    color: rgba(255, 255, 255, 0.85);
  }
  .cta-actions {
    display: flex;
    gap: 0.7rem;
    justify-content: center;
    flex-wrap: wrap;
  }
  .btn-light {
    background: #fff;
    color: var(--green-dk);
  }
  .btn-outline {
    background: transparent;
    color: #fff;
    border-color: rgba(255, 255, 255, 0.5);
  }
  .btn-outline:hover {
    border-color: #fff;
  }

  /* ---------- Responsive ---------- */
  @media (max-width: 820px) {
    .hero {
      grid-template-columns: 1fr;
      gap: 2rem;
      padding-top: 0.5rem;
    }
    .hero-card { order: -1; }
    .map-card { transform: none; max-width: 420px; }
    .split { grid-template-columns: 1fr; }
    .stats { grid-template-columns: repeat(2, 1fr); gap: 1.5rem 1rem; }
  }
  @media (max-width: 520px) {
    .search { flex-direction: column; }
    .search-fields { flex-direction: column; align-items: stretch; gap: 0.5rem; }
    .input-icon input {
      border: 1px solid var(--line-2);
      border-radius: var(--radius-sm);
      background: var(--paper-2);
    }
    .search-arrow { display: none; }
    .search-btn { width: 100%; }
    .section-head.row-head { flex-direction: column; align-items: flex-start; }
  }
</style>
